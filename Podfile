# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SimpleNote-VIPER' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'RxSwift', '~> 4.1.2'

  # Pods for SimpleNote-VIPER

  target 'SimpleNote-VIPERTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SimpleNote-VIPERUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
